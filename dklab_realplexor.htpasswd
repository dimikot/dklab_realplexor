# Format of each line - "login:password_hash". You may use standard 
# Apache "htpasswd" utility to work with this file.
#
# Default user is the user with empty login and password. 
# So, if this file contains ":" line, that means we allow
# access without password by default. If no such line is
# presented in the file, anonymous access is not allowed.

# Allow access without logon.
:
